<div id="what" class="what section">
	<div class="lead">
		<h2 class="lead-title">What kind of code?</h2>
	</div>
	<h3 class="experience margin-bottom-30">Experience</h3>
{# 	<div class="grid grid-container">
		{% for item in what_section.experience %}
		<div class="item -what -twelve">
			<h4 class="heading">{{ item.heading }}</h4>
			<p class="copy">{{ item.copy }}</p>
		</div>
		{% endfor %}
	</div> #}
	<!-- Slider main container -->
	<div class="swiper-container mobile-only">
		<!-- Additional required wrapper -->
		<div class="swiper-wrapper">
				{% for item in what_section.experience %}
					<div class="swiper-slide item -what -twelve">
						<h4 class="heading">{{ item.heading }}</h4>
						<p class="copy">{{ item.copy }}</p>
					</div>
				{% endfor %}
		</div>
		<div class="swiper-scrollbar"></div>
	</div>
</div>
